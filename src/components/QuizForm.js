import React from 'react';

const QuizForm = ({
                      onSubmit,
                      onChange,
                      errors,
                      successMessage,
                      user
                  }) => {

    return (
        <div></div>
    );
};

export default QuizForm;
